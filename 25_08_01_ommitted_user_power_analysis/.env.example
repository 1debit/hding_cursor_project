# Snowflake Connection Configuration
# Copy this file to .env and fill in your actual values

# === Required Connection Settings ===
SNOWFLAKE_ACCOUNT=your_account.region.snowflakecomputing.com
SNOWFLAKE_USER=your_username
SNOWFLAKE_ROLE=ANALYST_ROLE
SNOWFLAKE_WAREHOUSE=COMPUTE_WH
SNOWFLAKE_DATABASE=ANALYTICS
SNOWFLAKE_SCHEMA=PUBLIC

# === Authentication Method 1: Password ===
# Use either password OR private key authentication (not both)
SNOWFLAKE_PASSWORD=your_password

# === Authentication Method 2: Private Key (Recommended for Production) ===
# SNOWFLAKE_PRIVATE_KEY_PATH=~/.ssh/snowflake_rsa_key.p8
# SNOWFLAKE_PRIVATE_KEY_PASSPHRASE=your_passphrase

# === Optional Settings ===
# Query tag for tracking/auditing (appears in QUERY_HISTORY)
QUERY_TAG=cursor-analyst-starter

# === Environment-specific overrides ===
# Uncomment and modify for different environments:
# DEV_WAREHOUSE=DEV_WH
# PROD_WAREHOUSE=PROD_WH
# TEST_DATABASE=TEST_DB

# === Notes ===
# - Never commit .env file to version control
# - Use private key authentication for production environments  
# - Set appropriate warehouse size: XS/S for exploration, M+ for heavy jobs
# - Consider using different warehouses for different workloads
